@model IEnumerable<PNTS.Models.Register>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.Title)
        </th>
        <th>
            First Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.FirstMidName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RegisterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RegisterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RegisterID })
        </td>
    </tr>
}

</table>
